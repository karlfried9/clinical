- if user_signed_in?
  %li= link_to t('menu.sign_out', :default => 'Sign out'), destroy_user_session_path, :method=>'delete'
- else
  %li= link_to t('devise.sessions.new.sign_in', :default => 'Sign in'), new_user_session_path
  //%li= link_to t('devise.registrations.new.sign_up', :default => 'Sign up'), new_user_registration_path


